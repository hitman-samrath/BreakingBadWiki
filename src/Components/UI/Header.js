import '../../App.css'
import logo from '../../Assets/logo.png'

const Header = () => {
    return (
        <header className='logo'>
            <img src={logo} alt='Logo'/>
        </header>
    );
}

export default Header
